# GitHub CLI non-auth config. Auth lives in ~/.config/gh/hosts.yml and should
# not be committed.
version: 1
git_protocol: https
editor:
prompt: enabled
pager:
aliases:
    co: pr checkout
http_unix_socket:
browser:
